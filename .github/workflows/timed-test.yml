name: Test Schedule

on:
  workflow_dispatch:
  schedule:
    - cron: "*/10 * * * *"

jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - name: Test Schedule Job
        run: echo "This job was automatically run!"
